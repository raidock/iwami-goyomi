# 石見暦（いわみごよみ）

島根県石見地域（浜田市・江津市ほか）の催しを、市町や観光協会の公式サイトから
自動で集めて公開する個人プロジェクト。運営は TandN Design（一人）。

公開先: https://raidock.github.io/iwami-goyomi/

**このファイルは、過去に実際に起きた事故と、その結果決まった方針の記録です。**
コードだけ読むと「改善できそう」に見えて、実は事故の再発になる箇所が多くあります。
変更を提案する前に、必ず「譲れない設計判断」と「これまでに起きた事故」を読んでください。

---

## 何をするツールか

市町のお知らせは、体感で8〜9割が入札・人事・計画案・議事録といった行政内部の情報です。
住民向けの催しはそこに埋もれています。このツールは**その中から催しだけを掘り出します**。

- 収集: 8情報源のRSS（浜田市 / 江津市 / 益田市 / 大田市 / はまナビ / 江津市観光協会 /
  大田市観光協会 / 邑南町観光協会）
- 仕分け: ルールベースの分類器。実運用のノイズ率は約80〜89%
- 日付: 仕分けを通過したものだけ詳細ページを見て、開催日と申込締切を抽出
- 承認: **人が最終判断する**（自動公開されるのは確度の高いものだけ）
- 公開: 静的HTML + GitHub Pages。毎朝6時（JST）に自動収集

---

## 譲れない設計判断（変更前に必ず読む）

### 1. 種別は3つ。時間軸が違うので分けている

| 種別 | 時間軸 | 並び順 | 住民の動詞 |
|---|---|---|---|
| 催し | 開催日 | 日付の昇順 | 行く |
| 募集 | **締切** | **締切の昇順（逆順）** | 申し込む |
| 制度 | なし | カテゴリ順 | 使う |

**募集だけ並び順が逆**なのは意図的です。締切が近いものほど上に来ないと役に立ちません。
締切が14日以内のものがあるときだけ、募集ブロックが催しより上に繰り上がります。

判定順序にも意味があります。**制度 → 催しの名詞 → 募集の語** の順。
「出前講座を**ご利用ください**」は講座の語を含むが制度。
「さざんか祭り 参加者**募集**」は本体が祭りなので催し。

### 2. uid は URL 基準。後から埋まる情報を混ぜない

日付を uid に含めていたため、日付抽出で開催日が埋まった瞬間に uid が変わり、
**同じ催しが2枚並ぶ事故**を起こしました（v1.5）。同時に、締切だけ取れたものは
「既知」と判定され、抽出結果が捨てられていました。

`tests/test_dedup.py` が回帰テストです。**uid にタイトルや日付を入れないこと。**

ICS の UID（`{uid}@sanin-nomi`）と PRODID も同じ理由で変更しません。配信済みの
`events.ics` の購読者のカレンダーで、既存の予定が消えて別物として再追加されるためです。
名前が石見暦に変わっても据え置きます（蚤の市時代の名残に見えますが、直しません）。

### 3. 承認キューは残す。全自動にしない

自動公開されるのは確度の高いもの（score 4以上）だけで、それ以外は人が見ます。
これは「作りが甘い」のではなく、**公開物を一人で運用するうえで品質を守る唯一の
現実的な方法**として意図的に残しています。1日5分の作業です。

**承認そのものをエージェントやLLMに任せないでください。** ここが最後の砦です。

### 4. 終わった催しは消さない

開催日・締切が過ぎたものは「これからの催し」から外れますが、ページ下部の
畳んだ節に残ります。**削除しません。**

暦は、これからの予定表であると同時に、過ぎたことの記録でもあります。
過疎地では、祭りが縮小し、統合され、やがて途絶えていく過程が、どこにも記録されない
まま失われます。それを残すことがこのサイトの存在理由の半分です。

判定で注意する点:
- **日付が分からないものは終了扱いにしない**（勝手に消してはいけない）
- **期間ものは終わりの日で見る**（「石見神楽定期公演 2026.4〜2027.3」は開始日が過去でも開催中）

**ただし、一度も載っていないものを終了済みとして足すことはしません。**
残す価値は「載っていたものが終わった」という過程にあります。一度も載らなかった
ものには、その過程がありません。畳んだ節が膨らむだけです。

そこで `ReviewQueue.is_finished()` が、**初めて見るもので、分かっている日付が
全部過去のもの**を取り込みません。**既知には適用しません**（一度載ったものは畳んで残す）。
フィードの窓を広げると終了済みが大量に流れ込むので、この2つは対になっています。

`publish.is_past()` とは基準が違います。あちらは**画面での見せ方**（種別ごとに見る
欄を変える）、こちらは**そもそも取り込むか**。名前が似ているので混ぜないこと。

### 5. 生活インフラ情報は載せない

断水、通行止め、ごみ収集の変更、水道メーター取替などは対象外です。

線引きは **「横断して集めることで価値が出るか」**。
浜田の人が江津の祭りを知りたいから、9市町を横断する意味があります。
断水は自分の町のことしか関心がなく、市の公式サイトを見れば済みます。
これを入れると「市サイトの劣化コピー」に近づき、しかも一次情報より遅れます。

迷ったときは、この基準に当ててください。ごみ収集日も除雪情報も同じ理由で外れます。

### 6. 依存は最小に保つ

`requests` / `beautifulsoup4` / `lxml` / `PyYAML` の4つだけ。個人が10年単位で
運用するには、依存の少なさが最大の防御です。

**追加しないこと。** とくに `dateutil`（日付処理は自前の関数で足りている）、
`feedparser`、`pandas` は誘惑が強いですが不要です。
`zoneinfo` のような標準ライブラリは可。

### 7. 保存は JSON。Git で差分が見える形にする

`./data/` に `pending.json` / `approved.json` / `rejected.json` を置きます。

DBに移すと Git の差分が読めなくなります。数千件までは JSON で問題ありません。
超えたら SQLite を検討しますが、**いま移す必要はありません。**

**承認データの置き場は `./data` ただ一つ。**
かつて `~/iwami-events-data` に逃がしていたが、CI が `./data` を使うため
キューが2つに分裂し、手元の collect が公開に反映されない状態が続いた。
**置き場を増やさないこと。**

### 8. 分類器はルールベース。LLM に置き換えない

`collector/classify.py` の除外語・イベント語の一覧は、**1つ1つが実際に起きた
誤判定の記録**です。LLMに置き換えると、この蓄積が失われ、コストも発生し、
判定が再現しなくなります。

精度は実データで測定済み（浜田67件+益田2件で再現率100%・適合率100%）。
**変更したら必ず測り直してください。**

### 9. 日付は日本時間で判定する

GitHub Actions のサーバーは UTC です。`date.today()` をそのまま使うと、
日本時間の0時〜9時のあいだ日付が1日ずれ、「あと○日」も終了判定も狂います。

**必ず `collector/models.py` の `today_jst()` / `now_jst()` を使ってください。**

### 10. カテゴリはタイトルを優先する

説明文まで見ると引っ張られます。「ぶどうまつり」の紹介文に
「石見神楽の上演もあります」とあるだけで神楽カテゴリになりました。
祭りの紹介文はたいてい石見神楽に触れるので、この誤りは多発します。

### 11. 観光協会は `trust: high`

観光協会のイベントカテゴリRSSには、そもそもイベントしか流れてきません。
自治体サイトと同じ厳しさで仕分けると、語彙にない催しを取りこぼします。
そのため `config.yaml` で `trust: high` を指定し、しきい値を下げています。

ただし信頼度を上げると**フィードの選び方がより重要**になります。
江津市観光協会はサイト全体のRSSしかないため、`url_include: "/news/"` で
観光スポット紹介ページを除外しています。

### 12. 情報源への礼儀

- 詳細ページを見に行くのは**仕分けを通過したものだけ**（約200件ではなく約40件）
- 取得間隔は既定1秒（`config.yaml` の `fetch_delay_sec`）。**情報源ごとに上書きできる**
  ので、robots.txt が `Crawl-delay` を宣言していたらその情報源にだけ書く
  （全体を上げると関係のない情報源まで遅くなり、収集が終わらなくなる）
- User-Agent は正直に名乗る（`collector/__init__.py` の `USER_AGENT` 1か所）
- **本文は転載しない。** 保存するのはタイトル・日付・リンクのみ
- 全件に一次情報リンクと掲載日を表示する

### 13. 語彙には適用範囲がある。タイトル向けの語彙を本文に当てると壊れる

除外語・過去の報告・カテゴリの語彙は、いずれも**タイトルとRSS要約に向けて育てたもの**です。
ページ全文には行政語（実行委員会・入札・契約）が普通に混ざるため、本文に当てると
本物の催しが落ちます。**祭りのページに「実行委員会」と書いてあるのは当たり前**で、
これは語の問題ではなく適用範囲の問題です。

同じ型を4回踏んでいます。

| 回 | 何が起きたか |
|---|---|
| 1 | カテゴリを本文まで見て「ぶどうまつり」が神楽・伝統芸能になった |
| 2 | `PAST_REPORT` を本文に当てると「昨年はご参加いただきました」で人気イベントが消える |
| 3 | 除外語を本文に当てると「実行委員会」で大田市最大の祭り（天領さん）が消える |
| 4 | 種別を本文まで見て、RSS要約の「貸出」1語で三瓶山のハンモックが制度になった |

語彙を新しい場所（本文・別のフィード・別の市町）に適用するときは、**まず実データで
測ること。** 3回目は測定で未然に防ぎました（`score = 0` の105件で検証）。

4回目で分かったのは、**危ないのは「語彙を広げるとき」だけではない**ことです。
種別の判定は最初から本文を見ていて、誰も広げていません。**いま本文を見ている所が
残っていないか**を疑ってください（`detect_kind()` がそうでした）。

#### 「本文」には2種類ある。危険度が違う

| どの本文か | 中身 | 除外語を当てると |
|---|---|---|
| **RSS要約**（`classify.py` が見ている） | 記事の書き出し数百字。話題が絞られている | 実測で**実害0件**（2026-07-31 / 322件） |
| **詳細ページ全文**（`extract.py` が見ている） | 問い合わせ先・実行委員会・関連記事一覧まで含む | 天領さんが「実行委員会」で消えた |

**この設計判断が指しているのは後者です。** 「本文を見ている」と議論するときは、
**どちらの本文かを先に決めてください。** 混ぜて語ると危険度を読み違えます
（上の4回のうち、1・2・4は RSS要約、3は詳細ページ全文の話です）。

#### 棚卸しの測定結果（2026-07-31 / 実データ322件）

`classify.py` の本文参照を、タイトルだけに切り替えたときの影響です。
**次に同じ議論をするとき、ゼロから測り直さずに済むように残します。**

| どこを切るか | 何が起きるか | 判断 |
|---|---|---|
| **拾う側**（`EVENT_MEDIUM` `EVENT_STRONG_RE` `GENERIC_ACTION_RE` `EVENT_WEAK`） | **本物が1件消える**（大田市中央図書館「写真で知ろう！おおだの自然」）／auto→review が11件増える | **触らない** |
| **捨てる側**（`PAST_REPORT` `HARD_EXCLUDE` `HARD_EXCLUDE_RE`） | 実害0件（本文だけで落ちている8件は全部ノイズと確認）／承認作業が8件増える | **触らない** |

捨てる側の8件のうち4件は、本来タイトルで落とせるはずが**語彙が足りず本文に頼って
いた**ものです（`委員・…推進員を募集` が中黒で切れる、`公募`・`補助金` が未収録）。
語を足すかは別途測ること。**`公募展` は美術の催し、`補助金説明会` は本物の催し**なので、
素朴に足すと本物が消えます。

### 14. 構造化されていても、それが何を指しているかは別

「サイトが機械可読な形で持っているなら、人手の本文より優先する」は正しい判断です。
ただし**形式は意味を保証しません。**

`<time datetime="2026-07-08">` は「これは日付だ」としか言っていません。江津市観光協会
ではそれが**記事の掲載日**でした。期間として読むと7件すべての開催日が掲載日に化けます
（キッズフェス in GOTSU が 7/18 → 7/8）。採用したのは `class="period_box"` だけで、
中身が「イベント期間 2026年07月18日(土) ～ 08月09日(日)」だと実データで確かめています。

`class="-sidebar-on"` が枠ではなく**レイアウト状態**を示していたのと同じ型です。
名前や形式ではなく、**実データで何を指しているかを見てください。**

### 15. 変わる事実を2箇所に書かない

同じ事実を2箇所に書くと、片方が必ず腐ります。**参照させるか、数を書かないか**の
どちらかにしてください。

| 何を2箇所に書いたか | どうなったか |
|---|---|
| 承認キューの置き場（`~/iwami-events-data` と `./data`） | 手元の作業が公開に反映されない状態が続いた |
| `AGENTS.md` に CLAUDE.md の内容を写す | 古い方針で作業することになる（いまは転送メモだけにしてある） |
| `field-checker.md` に設計判断と事故の**件数**を書く | 12と11のまま止まり、実際は増え続けた |

件数・一覧・置き場所は、**CLAUDE.md か config.yaml のどちらか1つ**に置きます。

### 16. 手で足す口は1つだけ持つ（`data/manual.json`）

自動収集に乗らない催しは必ず出ます。**そのとき載せる手段が一つも無かった**ので
作りました（問い合わせフォームも掲載方針も削除依頼の窓口も用意したのに、
**追加だけが欠けていた**）。

乗らない例は実在します。はまナビの `/lp/` は **WordPress の外にある静的HTML**で、
CMSに存在しないので**どんなフィードにも絶対に出ません**（5本あり、うち催しは
浜っ子夏まつり1本。トップページからのリンクが唯一の入口）。

**ただし、浜っ子夏まつりが載らなかった原因はLPではありません**（下の事故表を
読んでください）。手動掲載は**穴を塞ぐ道具ではなく、最後の手段**です。
仕組みの穴を手作業で埋め始めたら負けなので、原因を先に潰してください。

- 置き場は `data/manual.json` **ただ一つ**（設計判断7と同じ。増やさない）
- **収集は読むだけで、書き換えません。** 手で入れた値は機械の抽出より優先です
  （設計判断3「人の判断は上書きしない」の延長）
- 合流は `build` のとき。同じURLが自動収集で来たら**手動が勝ち**、自動側は
  公開データにも承認キューにも入りません（同じ1件を2回判断させない）
- `source: 手動` / `date_source: 手入力` は書き手に任せず必ず入れます。
  根拠が追えることは公開物の生命線です
- 書き間違いは警告して**その1件だけ**飛ばします。**公開全体は止めません**
  （1件の打ち間違いで暦が丸ごと消えるほうが痛い）

書き方は `data/manual.example.json`。回帰テストは `tests/test_manual.py`。

---

## これまでに起きた事故（ほぼすべて実際に発生）

**20件すべて、テストは緑でした。** 実データか公開画面を見て初めて分かったものばかりです。
これがこのプロジェクトで最も重要な教訓です。

**最後の1件だけ性質が違います。** 他はすべて「間違って載った」ですが、あれは
「**載らなかった**」。画面を見ても気づけません。**無いものは目に入らない**からです。
測ってきたのは全部「拾ったものが正しいか」で、「載るべきものが載っているか」は
一度も測っていませんでした。

末尾の**（未然）**2件は公開前に測定で防いだものですが、**同じ罠がまだそこにある**ので
警告として並べています。事故表の値は「壊れた記録」より「こういう罠がある」ことにあります。

| 事故 | 原因 | 対策 |
|---|---|---|
| 「水道メーターの取り替えを行います」が自動公開された | 「行います」を強い開催表現にしていた | 汎用動詞は催しの名詞を伴うときだけ強い根拠にする |
| 同じ催しが2枚並んだ | uid に開催日を含めていた | uid は URL 基準に |
| 締切だけ取れた分が捨てられた | 既知のものを更新せず飛ばしていた | 既知には新情報を追記する（人の判断は上書きしない） |
| 終わった花火大会が「これから」に残った | 過去判定を書いていなかった | `is_past()` を実装し、畳んだ節へ |
| 「あと○日」が1日ずれた | GitHub Actions が UTC | `today_jst()` に統一 |
| 「ぶどうまつり」が神楽カテゴリに | 説明文まで見ていた | カテゴリはタイトル優先 |
| 「意見**を**募集します」がすり抜けた | 助詞が挟まると文字列一致が漏れる | 揺れやすい語は正規表現で |
| 「出場者**大**募集」を取りこぼした | 同上 | 同上 |
| 江津の RSS が見つからなかった | `<head>` ではなく本文の `<a>` にあった | 本文も走査し「新着」を優先 |
| 観光協会の日付が全く取れなかった | 和暦（令和7年）・略記（R8.7.20） | `wareki_to_seireki()` で吸収 |
| ワークフローが3行欠落して2日間動かなかった | 複数行の貼り付けで行が落ちた | ワークフローのYAMLをテストで検証する |
| 「〜に参加しました！【お礼、報告】」が承認キューに溜まった | イベント語（相談会・フェア）を含むので score 3 が付いていた | 参加報告は `PAST_REPORT_TITLE` で落とす。**タイトルだけで見る**（案内文の「昨年は多くの方にご参加いただきました」で本物が消えるため） |
| 承認キューが2つに分裂し、手元の作業が公開に反映されなかった | ローカルは `~/iwami-events-data`、CI は `./data` を見ていた | `./data` に一本化 |
| 大田市観光協会の全記事に同じ締切 2026-08-30 が付いた | ページ下部の「ほかの催し」一覧（`div#sub_events_area`）にある他記事の見出しから「〜まで」を拾っていた | 関連記事一覧を本文から除去する（`_NOISE_JOINED`） |
| 期間の終わりが開始より前になり「8月18日〜8月17日」と表示された | ページ側が「8月31（金）」と日を落としており、同じ節の注記「※8月10日～8月17日は開催しません」の日付を終わりとして拾った | `date_end < date_start` なら終わりを捨てる（**出口で結果を検証する**。入口を直すと `8月31日〜9月1日` を誤読しかねない） |
| 「締切あり」タグが、締切と無関係のものを表示していた | タグはRSSタイトルの「まで」から、締切は詳細ページから取っており、**両者が一度も噛み合っていなかった**（締切が取れている16件にタグは0件、タグが付いた2件はどちらも会期末の「まで」） | タグを廃止。**表示している値が何から計算されているかを確かめる**（名前が一致していても、入力が違えば別のものを指す） |
| 会議体を名乗る主催者の催しが、加点される前に消えていた | `委員会` `協議会` `審議会` を素の除外語に置いており、さらに `議会` がその2語に部分一致していた。**会議体は催しの主催者名として普通に現れる**（教育委員会主催の講座、実行委員会からのお知らせ、観光協議会主催のウォーク） | 催しの名詞を伴わないときだけ落とす（`SOFT_EXCLUDE`）。**除外語は加点より先に効くので、主催者名に使われる語を入れてはいけない** |
| 浜田市最大の祭り（石州浜っ子夏まつり）が、前日まで載っていなかった | **フィードの窓から落ちていた。** はまナビの event フィードは10件しか出さず、投稿頻度が高いので**窓は約2週間分**しかない。記事は 7/2 に出ていて（`/archives/36283`・分類器も score 3 で通す）、石見暦の初版 7/28 には既に窓の外だった。**LPのせいではない**（LPもあるが記事も存在した）。人伝に聞くまで気づけなかった | 当座は `data/manual.json` で手当て（設計判断16）。**原因は窓なので手作業では埋まらない** — `?paged=N` で窓を広げる／`audit` で全在庫と突き合わせる |
| **（未然）** 本文に除外語を当てると大田市最大の祭り（天領さん）が消えるところだった | 除外語彙はタイトル向けに育てたもので、本文には行政語（実行委員会・入札）が普通に混ざる | 語彙の適用範囲を変えるときは実データで測る（設計判断13） |
| **（未然）** 除去語に `sidebar` を足せば公開中5件の本文が消えていた | 江津市観光協会は記事本文を包む div に状態クラス `-sidebar-on` を付けている。**枠の名前と、枠の有無を示す名前は違う** | 除去語は1語ずつ実データで測ってから足す。損失が出た語は入れない |

### 日付表記は7通りある

| 表記 | 例 | 出どころ |
|---|---|---|
| 西暦 | 2026年8月3日 | 市の公式 |
| 和暦 | 令和7年11月8日 | はまナビ |
| 略記 | R8.7.20 | 江津観光協会 |
| 見出し直下 | 「提出期限」→ 8月3日 | 江津市 |
| 流し込み | 7月31日（金）までに | 浜田市 |
| 【】ラベル | 【日時】令和7年… | はまナビ |
| タイトル冒頭 | 8月22日（土）有福温泉… | はまナビ |

1ページに日付が7個あることもあります（江津 Go-Con: 掲載日/提出期限/公募期間/
結果通知/勉強会×3/最終審査会）。**本文を正規表現でなめると必ず間違えます。**
見出しの語が日付の意味を教えてくれるので、見出しベースを優先します。

---

## 構成

```
main.py                    collect / pending / review / build / dedupe / audit / health / status
collector/
  classify.py              仕分け器と種別判定（このプロジェクトの心臓）
  extract.py               詳細ページからの日付・締切抽出（見出しベース＋文脈語ベース）
  models.py                Event モデル / 日本語日付パーサ / 和暦 / 日本時間
  review.py                承認キュー（pending / approved / rejected）
  manual.py                手で足す掲載（data/manual.json。収集は書き換えない）
  publish.py               公開サイトのレンダラー
  about.py                 掲載方針ページ
  filters.py               山陰フィルタ（蚤の市版の名残）
  renderers.py             JSON / ICS 出力
  sources/
    base.py                アダプター共通
    municipal_rss.py       自治体・観光協会の汎用RSSアダプター（フィード自動発見）
    oda_city.py            大田市（フィードに掲載日が無いので新着一覧から補う）
    good_antiques.py       蚤の市アダプター（旧版から継続）
tests/                     すべて実データ由来（一覧は「開発のしかた」。数は書かない）
config.yaml                情報源・サイト設定
.github/workflows/         毎朝6時 + push で動く
```

---

## 開発のしかた

### テストは実データ由来。合成データに置き換えない

`tests/` のケースは、実際に取得したタイトルや実ページの構造を写したものです。
「きれいなテストデータ」に書き換えると、**事故の記録が失われます。**

```bash
python tests/test_classify.py       # 実データ69件で再現率・適合率を測る
python tests/test_gotsu_cases.py    # 江津の実データ10件
python tests/test_kinds.py          # 種別・並び順・過去判定・時差
python tests/test_extract.py        # 詳細ページからの日付抽出
python tests/test_wareki.py         # 和暦・略記・タイトル冒頭
python tests/test_dedup.py          # uid 安定性（重複事故の回帰）
python tests/test_review_refresh.py # 既知への追記・上書きの線引き
python tests/test_manual.py         # 手で足す掲載（収集で消えない・同じURLは手動が勝つ）
python tests/test_feed_pages.py     # フィードの窓（?paged=N）と、終わったものを取り込まない規則
python tests/test_similar_titles.py # 市またぎ重複の警告（表示専用）
python tests/test_feed_discovery.py # RSS自動発見
python tests/test_good_antiques.py  # 蚤の市パーサ
python tests/test_workflow.py       # ワークフローのYAML（CIは手元で動かないので形を守る）
python tests/test_fetch_delay.py    # 取得間隔が情報源ごとに効くか（Crawl-delay 対応）
python tests/test_oda_city.py       # 大田市の掲載日補完（フィードに日付が無い）
```

### 分類器を変えたら必ず測り直す

`tests/test_classify.py` が再現率と適合率を出します。
**取りこぼしゼロを最優先**にしてください。公開物では「載っていない」ほうが
「間違って載っている」より痛いためです。迷ったものは捨てずに承認キューへ送ります。

### テストが緑でも、実物を見る

このプロジェクトの事故は全部これでしか見つかりませんでした。

```bash
python main.py collect     # 実際に取得して仕分ける
python main.py pending     # 承認待ちを一覧で見る
python main.py build       # サイトを生成
open out/index.html        # 画面を見る ← これを飛ばさない
```

### 収集の変更は、`data/` を複製して測る

収集の挙動を変えたら、**本番の `data/` に入れる前に数字を出します。**
`--data-dir` があるので、複製して回せば公開データに触れずに済みます。

```bash
cp -R data /tmp/data-trial
python main.py collect --data-dir /tmp/data-trial   # ここで測る
# 公開中・承認待ち・却下済みの増減と、消えたものが無いかを突き合わせる
```

**同じ収集を2回走らせないこと。** 相手のサーバを2回叩くことになります（設計判断12）。
数字が良ければ複製をそのまま `data/` へ移せば済みます。

### 分類器を直しても、画面はすぐには変わらない

**push では収集が走りません。** ワークフローの収集ステップには
`if: github.event_name != 'push'` が付いています。コードを直すたびに情報源を
叩かないための設計です（設計判断12）。push で走るのはサイト生成だけです。

一方、分類器の出力（`kind` `tags` `category` など）が公開データに反映されるのは
**収集のとき**です（`ReviewQueue.CLASSIFIED`）。つまり:

| いつ | 何が起きるか |
|---|---|
| コードを push | サイトを生成し直すだけ。**分類は古いまま** |
| 毎朝6時（JST）の定時収集 | 分類し直して公開データを更新 |

**「直したのに画面が変わらない」のはこのためです。** 最大1日ずれます。
すぐ反映したいときは手元で `python main.py collect` を回してコミットします
（詳細ページを踏みたくないときは `--no-fetch`）。

なお**RSSから落ちた古い記事は、収集しても更新されません**（対象に入らないため）。
終わった催しは消さない方針（設計判断4）なので、古い分類のまま残る記事は増えていきます。

### 情報源を追加するとき

`config.yaml` に3行足すだけで動きますが、**必ず実際に取得して確認**してください。
市町ごとに RSS の置き場所も日付の書き方も違います。

```yaml
municipalities:
  - key: masuda_city
    municipality: 益田市
    site: https://www.city.masuda.lg.jp
    # feed_url を省略すると自動発見します
```

追加後の確認:
1. `python main.py collect` のログで RSS が見つかったか
2. 取得件数とノイズ率（0件なら構造が違う）
3. `python main.py pending` で拾ったものの質
4. 日付が取れているか（取れないなら、その市町固有の表記がある）

---

## 人が担う部分（自動化しない）

- **承認** — 1日5分。品質を守る最後の砦
- **削除依頼への対応** — 理由を問わず速やかに。`review` で `n` を押せば
  却下済みに記録され、二度と再掲載されない
- **問い合わせへの返信** — Googleフォーム経由で届く
- **掲載の追加** — 「うちの催しを載せて」に応える口。`data/manual.json` に
  書いて `build`（設計判断16）。自動収集に乗らない催しはここからしか載らない

---

## コミット規約

英語プレフィックス + 日本語の説明。論理単位で分ける。

```
feat: 益田市を情報源に追加
fix: 和暦の略記を読めるようにする
chore: 掲載を承認
chore: 主催者の依頼により掲載を取り下げ
```

自動収集のコミットは `chore: 収集 YYYY-MM-DD`（作者 `iwami-goyomi-bot`）。

**作業前に必ず `git pull`。** 毎朝ボットがコミットするので、手元は毎日古くなります。

---

## いま分かっている課題

**残っている課題はここが正。** 片付いたら消し、見つけたら足します。

**順序は書きません。** 優先順位はそのときの状況で変わるので、正の側に書くと腐ります
（順序は `NEXT.md` で都度指示されます）。日付で節を分けるのもしません。
古い節が読まれなくなるためです。

### 取りこぼし（載るべきものが載っていない）

**測る手段は `python main.py audit`。** 情報源の在庫とキューを突き合わせ、
見ていない記事だけを出す（読むだけ・月1回・人が見る）。
**外部の催し一覧と比べるより、情報源自身の在庫と比べるほうが正確**です。


**フィードには窓がある。窓より古い記事は、一度も目に入りません。**
そして **大きな催しほど早くから告知されるので、窓から落ちやすい**。
浜っ子夏まつりは開催30日前の告知で、窓は14日分でした（事故表の最後）。

窓の広さは実測（2026-08-01）。**情報源を足したら必ず測ること。**

| 情報源 | 窓 | さかのぼれる期間 |
|---|---|---|
| 浜田市 | 77件 | 2022年から |
| 江津市 | 100件 | 約4か月 |
| **益田市** | **20件** | **約4週間** |
| 大田市 | 901件 | （掲載日なし。専用アダプターで補完） |
| **はまナビ** | **10件** | **約2週間** ← 投稿が多いので最も狭い |
| 江津市観光協会 | 9件 | — |
| 大田市観光協会 | 10件 | events_post は全15件 |
| 邑南町観光協会 | 10件 | 約4か月 |

**対策は入れた**（`config.yaml` の `feed_pages`。観光協会4つを3ページ＝約6週間分）。
`?paged=N` が効くのは **WordPress の観光協会だけ**で、**自治体3市は効かない**
（同じ内容がそのまま返る。実測）。効かないフィードは2ページ目で打ち切る。

窓を広げると終了済みがまとめて入るので、`ReviewQueue.is_finished` と対で使う
（設計判断4）。測定は 2026-08-01: 8ページで新規37件・終了として不採用75件・
実ページで確認した30件のうち誤りは0件。

残っていること:

- **益田市の窓（20件・約4週間）は広げる手段が無い。** paged が効かず、
  ほかの一覧も見つかっていない。いまは `audit` で「キューに無い」0件なので実害なし
- **在庫の拾い直し（`collect --pages 8`）はまだ実行していない。**
  実行すると公開+16件・承認待ち+21件（2026-08-01 実測）
- **`max_age_days: 400` が繰り返しの催しを切る。** 記事を作り直さない催し
  （毎週の天体観察会・毎月の朝市）は掲載日が何年も前のままなので足切りされる。
  大田市観光協会で3件。**情報源ごとに設定を分けるかは、測ってから決める**
  （浜田市の公式RSSには2022年のコロナ情報が残っていた。全体で伸ばしてはいけない）
- **記事の日付が古い繰り返しの催し**（温泉津あさいち。記事に `(7/26)` としか
  書いていない毎月の朝市）は終了判定で落ちる。いまは1件なので手動掲載で足りる
- **江津市観光協会と邑南町観光協会には `lp` 投稿タイプが登録されている**（いまは0件）。
  使われ始めたら**フィードには出せる**（はまナビの `/lp/` と違ってCMSの中）。ときどき見る
- **しまね観光ナビ**（`kankou-shimane.com`。県公式）は robots.txt が
  **`User-agent: ClaudeBot` を `Disallow: /`**。`*` の規定は無いので石見暦のUAは
  形式上は対象外だが、**エージェントに巡回させないこと。** 情報源にするかは人が判断する

### 分類器

- **天領さんのカテゴリが空** — 大田市最大の夏祭りだが、名前に「祭」が入らないため
  祭り・市・マルシェにならない。RSSに説明文も無いので、タイトルにも本文にも
  手がかりが無い。**実例が1件しかなく、規則を作る材料が存在しない。**
  1件だけを見て語を足すと、`体育` が「文化団**体育**成」に当たったように、
  後から反例が出る。**町を足して「〜さん」型が複数集まってから測ること。**
- **益田市の2件がカテゴリ未判定** — 「再エネ電力共同調達オークション実施説明会」
  「人権・同和教育2026」。候補は「説明会」「人権」だが、**語を足すと他市の判定にも
  効くので先に実データで測ること**（「相談」を入れたときに広く当たった前例がある）

### 日付・表示

- **公開中の催しの約半分が「日程は詳細ページで」** — 日付抽出の最大の改善余地。
  まず「なぜ取れないのか」を実物で診断すること（本当に書かれていないのか、
  書き方が想定外なのか）
- DANCE CONTEST の締切が取れない — 市のページが外部サイト（石央文化ホール）へ
  リンクしているだけ。リンク先をもう1段辿るかは未決
- **益田市6件のうち4件で開催日が取れていない** — 「約半分が日程は詳細ページで」と
  同じ傾向。益田市固有の日付表記があるかは実ページを見ないと分からない

### 運用・その他

- 承認が PC でしかできない — スマホ対応は未着手
- **残り4町（川本・美郷・津和野・吉賀）＋邑南町の公式は未着手** — 下調べ済み
  （2026-07-31）。邑南町は**観光協会だけ先に入れた**（公式はノイズ率75%で
  日付も取れないため、切り分けられるよう分けた）。
  5町とも同じIP（45.60.67.150）だが**CMSはばらばら**で、型は4通りある

  | 町 | 公式のフィード | 型 | 観光協会 | robots.txt |
  |---|---|---|---|---|
  | 邑南町 | `/rss/10/list1.xml` 40件・日付あり | **江津市と同型** | `ohnan-kanko.com/feed` 10件 | 404 |
  | 津和野町 | `/www/rss/news.rdf` 26件・日付あり | **浜田市と同型** | `tsuwano-kanko.net/feed/` 10件 | 404 |
  | 川本町 | `/feed/` 444件・**日付0件** | **大田市と同型の罠**（サイト目次） | `kawamoto-kankou.jp/feed/` 10件 | **Crawl-delay: 5** |
  | 美郷町 | `/feed` **6件**のみ | WordPress。量が少ない | `misato-kankou.com/feed/` 491件・日付100件 | 宣言なし |
  | 吉賀町 | **無し**（どの経路にも） | `/event-news/` のHTML一覧のみ | 見つからない | 404 |

  注意点:
  - **川本町の `/feed/` をそのまま使ってはいけない。** 大田市とまったく同じで、
    サイト全体の目次（「トップ」「行政」「アクセシビリティについて」）が444件並び、
    `<pubDate>` を1件も持たない。`/gyosei/` に日付つきの新着一覧があるので、
    `OdaCityRSS` と同じ方式で補える
  - **津和野町は自動発見が `emergency.rdf`（緊急情報）を拾う。** 新着は
    `news.rdf` なので `feed_url` を明示すること（本文の `<a>` に緊急のほうしか無い）
  - 美郷町は**公式より観光協会のほうが記事が多い**（6件 対 491件）。ただし
    **491件のうち日付を持つのは100件だけ**で、これは川本町・大田市の
    「サイト目次」と似た比率。**足すときは必ず中身を見ること**（先頭が
    「トップ」「お知らせ」で始まっているのも同じ匂い）
  - 吉賀町だけフィードが無い。`/event-news/` は**催し専用の一覧**（6件・日付つき）で
    信号は濃いが、件数が少なく更新も緩い（2023年の記事が残っている）
  - **おおちじかん**（`oochijikan.com`。邑南・川本・美郷の広域観光サイト）は
    **SSLエラーで取得できなかった**（2026-07-31）。3町を1つで賄える可能性があるので、
    **後日もう一度見る価値がある**。証明書の一時的な問題かもしれない

  邑南町の下調べ（2026-07-31。**まだ足していない**）:
  - 公式40件はノイズ率75%で、催しは実質2〜3件。**行政中心のフィード**
  - 観光協会10件は**ノイズ率0%・全部が催し**。`trust: high` に適する
  - **`日時：` を平文で流し込む型**（`■イベント概要` の下に
    `日時：令和8年10月17日（土曜日）18日（日曜日）`）。締切は `■申込締切` で
    取れるが、**開催日は取れない**
  - 観光協会は表組みで **`開催時期`** という見出しを使う。`HELD_HEADS` に無いため
    取れない（「平和と 美術と 音楽と」は 8/5〜8/9 なのに 8/9 だけ拾っていた）。
    **津和野町観光協会は表組みを使わない別型**なので、これは5町共通の手当てではない
  - **新しい型のノイズを1件見つけた。**「【大切なお知らせ】いわみスタジアム・
    瑞穂球場を改修します」が score 7 で自動公開される。RSS要約に
    「2030年に国民スポーツ大会が開催されます」と**改修の理由として**書かれているため。
    タイトルだけなら score 0。水道メーター事故と同型で、**施設のお知らせが
    本文で別の催しに言及する**という形
- **RSSから落ちた記事は、古い分類のまま残る** — 分類器の出力（`kind` `tags`
  `category` など）は再収集のたびに更新されるが、更新できるのは**その日のRSSに
  流れてきたものだけ**。フィードから落ちた古い記事は対象にならない。
  実例は「石見海浜公園 姉ヶ浜海水浴場 海開き」1件（カテゴリが `学び・講座` の
  ままで、いまの分類器なら `季節・自然`）。**終わった催しは消さない**（設計判断4）
  ので、こうした記事は増えていく
