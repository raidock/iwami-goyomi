# いま分かっている課題

**このファイルは「いま残っていること」です。片付いたら消してください。**
**方針・設計判断・過去の事故は `CLAUDE.md` にあります。**
**順序は書きません**（優先順位は `NEXT.md` で毎回指示されます）。

日付で節を分けるのもしません。古い節が読まれなくなるためです。
測定の数字は `measurements/` にあります。ここには**残っていること**だけを書き、
数字は写しません（設計判断15）。

---

## 取りこぼし（載るべきものが載っていない）

**測る手段は `python main.py audit`。** 情報源の在庫とキューを突き合わせ、
見ていない記事だけを出す（読むだけ・月1回・人が見る）。
**外部の催し一覧と比べるより、情報源自身の在庫と比べるほうが正確**です。

**フィードには窓がある。窓より古い記事は、一度も目に入りません。**
そして **大きな催しほど早くから告知されるので、窓から落ちやすい**
（浜っ子夏まつりがそうでした。`CLAUDE.md` の事故表）。

窓の広さは実測ずみ → [測定](measurements/2026-08-01-feed-window.md)。
**情報源を足したら必ず測ること。**

**対策は入れました**（`config.yaml` の `feed_pages`。観光協会4つを3ページ）。
窓を広げると終了済みがまとめて入るので、`ReviewQueue.is_finished` と対で使います（設計判断4）。

残っていること:

- **益田市の窓が最も狭い部類で、広げる手段が無い。** `?paged=N` が効かず、
  ほかの一覧も見つかっていない。いまは `audit` で「キューに無い」0件なので実害なし
- **江津市観光協会と邑南町観光協会には `lp` 投稿タイプが登録されている**（いまは0件）。
  使われ始めたら**フィードには出せる**（はまナビの `/lp/` と違ってCMSの中）。ときどき見る
- **しまね観光ナビ**（`kankou-shimane.com`。県公式）は robots.txt が
  **`User-agent: ClaudeBot` を `Disallow: /`**。`*` の規定は無いので石見暦のUAは
  形式上は対象外だが、**エージェントに巡回させないこと。** 情報源にするかは人が判断する

対処ずみ（消す前に、同じ話が出たとき困らないか確かめること）:

- ~~詳細ページの取得に失敗すると、終わった催しが「これから」に入る~~
  **対処ずみ**（2026-08-02）。**取れなかった新規は、その回は取り込まない。**
  取得は1回だけ取り直す（2回に増やさないこと。失敗するときは続けて失敗するので
  相手を余計に叩くだけ）。**見送るだけで、次の収集ではまた対象になる**
  （uid は URL 基準）。既知のものは対象外＝既にある日付を消さない
- ~~在庫の拾い直し（`collect --pages 8`）~~ **2026-08-01 に実行ずみ**
  （公開+21件・承認待ち+22件）。ふだんは `feed_pages: 3` で足りる

## 繰り返しの催し（実例が集まってから設計する）

**同じ形が複数出ています。** 定期市・定期公演は石見暦の看板領域なので、
まとまった数があるはずです。

| 実例 | どういう形か |
|---|---|
| 温泉津あさいち（大田市観光協会） | 毎月の朝市。**記事を使い回し、開催日だけ書き換える**（掲載日は 2025-08-31 のまま、開催日は令和8年7月26日）。次回の日付はページに書かれない |
| 三瓶の星空・天体観察会（毎週土曜／団体向け） | 掲載日が 2025-01 / 2024-05。**単一の開催日が無い**（「毎週土曜日」「水・木・金・日曜」） |
| サヒメルきっずサンデー（毎月第3日曜） | タイトルに直近の回（7/19）だけが載る |
| 石見神楽の定期公演（浜田・江津ほか） | 「2026.4〜2027.3」のような通年の期間で書かれる |
| みずほ夏祭り（邑南町観光協会） | 本文に「邑南町最大の花火大会。**毎年7月の第3土曜日**に開催されます」。単一の開催日が本文にしか無く抽出対象外なので、2026-07-18（今年の第3土曜）を過ぎても「これから」に残っている |
| 鹿子原の虫送り踊り（邑南町観光協会） | 本文に「**毎年7月20日**に行われる…五穀豊穣を願う民俗行事」。同じ理由で7/20を過ぎても残る |

**いまの仕組みで拾えない理由は2つ。** 別々の問題なので混ぜないこと。

1. **掲載日で足切りされる** — 記事を作り直さないので `max_age_days` を超える。
   大田市観光協会だけ `max_age_days: 1200` にして手当てした（実測: 在庫15件・
   最古821日前・ノイズ0件）。**全体では伸ばさない**（浜田市の公式RSSには
   2022年のコロナ情報が残っていた）
2. **単一の開催日が無い** — 「毎月第3日曜」を日付にできない。分かっている日付が
   直近の回（過去）だけだと、取り込みの終了判定（設計判断4）で落ちる

いまは**手動掲載**で個別に対応しています（温泉津あさいち）。ただし手動は
**そのURLを自動収集から締め出す**ので、ページ側が次回の日付に更新されても反映されません。

**日付の無い手動掲載が3件を超えたら仕組みを考えること**（2026-08-02 時点で1件）。

**いまは作らないこと。** 「毎月第3日曜」のような反復表現を扱う項目
（`recurrence` など。**現時点でコードには存在しません**）を設計するのは、
**町を足して実例が増えてから**。天領さんのカテゴリと同じで、1〜2件を見て
規則を作ると後から反例が出ます。

## 分類器

- **天領さんは記事が2枚あり、問題が別々**（`NEXT.md` と本ファイルで別の1枚を
  指していたのを、2026-08-06 にここへ揃えた）。

  | 記事 | url | category | 問題 |
  |---|---|---|---|
  | 大田市**公式**（親カード。「第４４回「天領さん」（２０２６）」） | `city.oda.lg.jp/update_info/10971` | **空** | RSSに説明文も無いので、タイトルにも本文にも手がかりが無い |
  | 大田市**観光協会**（久手会場。「【第44回 天領さん（久手会場）8/4…】」） | `ginzan-wm.jp/events_post/natsumatsuri-tenryosan-kute/` | 季節・自然 | 本文の「夜空を飾る光と音のファンタジー」「花火」「灯篭流し」から季節・自然に寄った。**大田市最大の夏祭りとしては祭り・市・マルシェが自然**だが、これも1件しか無い |

  **公式のほうが「実例が1件しかなく、規則を作る材料が存在しない」問題。**
  名前に「祭」が入らないため祭り・市・マルシェにならず、手がかりも無い。
  1件だけを見て語を足すと、`体育` が「文化団**体育**成」に当たったように、
  後から反例が出る。**町を足して「〜さん」型が複数集まってから測ること。**
  観光協会のほうは、本文の語彙が季節・自然に寄っている別の問題で、
  こちらも実例1件のままなので同じ理由で保留。
- **益田市の2件がカテゴリ未判定** — 「再エネ電力共同調達オークション実施説明会」
  「人権・同和教育2026」。候補は「説明会」「人権」だが、**語を足すと他市の判定にも
  効くので先に実データで測ること**（「相談」を入れたときに広く当たった前例がある）。
  **「人権・同和教育2026」は2026-08-07時点で詳細ページが404**（記事が消えている）。
  カテゴリを直す前に、いずれ却下に回すかどうかの判断が要る
- **邑南町公式の「移動献血を実施します」が、設計判断5の生活インフラに当たるか
  未確認** — 献血バスの日程は町ごとに別々（石見地域と瑞穂地域で年2回ずつ）で、
  横断して集める価値があるかは水道メーターや通行止めと同じ線引きの問題。
  2026-08-07に実ページを見て気づいた。既に載せている（1件）ので、
  除くなら却下、続けるなら現状維持の判断が要る
- **本文に頼って落としている4件は、本来タイトルで落とせる** — `公募`・`補助金` が
  未収録で、`委員・…推進員を募集` は中黒で切れる。ただし **`公募展` は美術の催し、
  `補助金説明会` は本物の催し**なので、素朴に足すと本物が消える
  → [測定](measurements/2026-07-31-classify-body-scope.md)

## 日付・表示

- **日付が取れない催し28件を診断ずみ**（2026-08-07。実ページを1件ずつ取得して
  10種類に分類） → [測定](measurements/2026-08-07-28-undated-events.md)。
  「見出しが構造化されていない（`<address>`タグ・番号付き平文段落）」が
  浜田市・大田市の2件で新しく見つかった。**実装はまだ**（測ってから決める）
- **巡回展は会場ごとに期間が並ぶ**（津和野町「津和野街道写真展」）。
  `【期間・会場】` の下に `7月10日(金)～7月26日(日) 日原公民館` が4つ続き、
  さらに「※11月以降の予定」と月だけの予告がある。**どれを開催日にするかが決まらない**
  （いまは何も取れず「日程は詳細ページで」）。**実例が1件なので、増えてから決める**
- **津和野町観光協会は本文に日付の見出しが無いページが多い** — 17件中9件しか取れず、
  うち5件はタイトルの `【…】8/15` 頼り。取れない8ページの原因は実物で分けてある。
  **一覧を除去しても増えないと分かっているので、そこから手を付けないこと**
  → [測定](measurements/2026-08-01-tsuwano-kanko-dates.md)
- **DANCE CONTEST の締切が取れない** — 市のページが外部サイト（石央文化ホール）へ
  リンクしているだけ。リンク先をもう1段辿るかは未決
- **益田市6件のうち4件で開催日が取れていない** — 「約半分が日程は詳細ページで」と
  同じ傾向。益田市固有の日付表記があるかは実ページを見ないと分からない
- **RSSから落ちた記事は、古い分類のまま残る** — 分類器の出力（`kind` `tags`
  `category` など）は再収集のたびに更新されるが、更新できるのは**その日のRSSに
  流れてきたものだけ**。フィードから落ちた古い記事は対象にならない。
  実例は「石見海浜公園 姉ヶ浜海水浴場 海開き」1件（カテゴリが `学び・講座` の
  ままで、いまの分類器なら `季節・自然`）。**終わった催しは消さない**（設計判断4）
  ので、こうした記事は増えていく
- **「次回9/19(土)」型のタイトルから日付が取れない**（実例:
  津和野夜神楽公演「【石見神楽】津和野夜神楽公演　次回9/19(土)公演」）。
  タイトル冒頭の型（`8月22日（土）…`）にも `【】` の後ろの型
  （`【津和野盆踊り】8/15`）にも当たらない、**日付表記10通り目の候補**。
  **実例が1件なので、いま `CLAUDE.md` の9通りには足さない**（設計判断13と同じ理由。
  増えてから測る）。2026-08-07に実ページを再確認したところ、本文には
  4月〜10月の全公演日が並んでおり、**タイトルの「次回9/19」がサイト側で
  既に絞り込んだ値**だった。取るならタイトル側からのほうが安全そう
- **`応募期日`の兄弟語（`申込期日` `受付期日` `提出期日`）はまだ実例が無い** —
  2026-08-07に`応募期日`を`DEADLINE_HEADS`に足すとき公開166件で一緒に測ったが、
  `受付期日` `提出期日` は0件、`申込期日` は1件だけあった（川本町公式。ただし
  フィードが無く自動収集が届かないページなので実害は無い）。既存の締切への
  誤爆は0件だったので、**次に実例が自動収集で見つかったときに足せばよい**
- **タイトル冒頭のスラッシュ日付（`10/12 市木市`）が非対応** — 既知の型は
  `8月22日（土）…`のような漢字表記のみ。実例は1件であり、しかも情報源側で
  既に「過去のイベント」ラベルが付いた10か月前の記事なので、直しても
  畳んだ節に移るだけで効果は薄い
- **日付が取れないまま古い記事が「これから」に残っている実例が複数ある**
  （2026-08-07 時点）。上の「繰り返しの催し」とは別の話 — こちらは**単発の
  記事が窓の広い収集で古いまま入り、日付が抽出できず終了判定（設計判断4）に
  乗らない**だけで、催し自体が繰り返されるわけではない。
  「10/12 市木市」（邑南町観光協会。掲載 2025-10-02、10か月前の記事）、
  「令和8年7月町政運営説明会の開催について」（邑南町公式。実際は7月14〜16日に
  3回開催して既に終わっている）、
  「GWの旅に…いま津和野で参加してほしい2つのアクティビティ」（津和野町観光協会。
  本文に「2026年GW」）

## 情報源

**石見9市町がそろいました**（2026-08-06。吉賀町を `data/manual.json` で追加）。
川本町・美郷町は**観光協会だけ**で、**公式はフィードが使えません。**

5町の調査結果と、その理由 → [測定](measurements/2026-08-02-five-towns.md)

残っていること:

- **吉賀町は手動掲載のまま**（アダプターは作らないと決めた。川本町と構造が
  違うので2つ要り、費用対効果が合わない）。2026-08-06 に実ページで再確認したが、
  [測定](measurements/2026-08-02-five-towns.md)の件数から増えていなかった。
  **記事が増えたら情報源として足すか測り直す**
- **おおちじかん**（`oochijikan.com`。邑南・川本・美郷の広域観光サイト）は
  **SSLエラーで取得できなかった**（2026-07-31）。3町を1つで賄える可能性があるので、
  **後日もう一度見る価値がある**。証明書の一時的な問題かもしれない

## 運用・その他

- **承認が PC でしかできない** — スマホ対応は未着手
- **`REPORT.md` は git に入っていません**（`.gitignore`）。毎回上書きするので、
  **前回の報告は残りません。** 残す価値のある測定は `measurements/` へ移すこと
- **節の並び順を「見出し文字列の出現順」で判定しているテストがある**
  （`tests/test_kinds.py` の `test_boshu_promoted_when_deadline_is_near` など）。
  本文以外の場所（ナビ・空メッセージ）に同じ語が入ると壊れる。実際に一度壊れた
  （2026-08-06。市町別の絞り込みを足したとき、空メッセージの文言に
  「これからの催し」という部分文字列が混ざり、本来の見出しより先に出現して
  順序判定を狂わせた）。**いまは直さない**（動いているものを触る理由が無い）。
  より確実な判定方法（節の順序を構造として取り出すなど）を思いついたら、
  そのとき提案する
- **`sitemap.xml` は作っていない** — ページが実質2枚（`index.html` `about.html`）
  なので発見の効果はほぼ無い。実装コストはほぼゼロなので、告知の直前くらいに
  作れば足りる → [調査](measurements/2026-08-06-json-ld-and-sitemap.md)
